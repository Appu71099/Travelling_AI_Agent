// Write tailwind config file to enable dark mode and extend the theme
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // make sure React files are included
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
